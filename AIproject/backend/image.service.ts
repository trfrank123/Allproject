import { Knex } from 'knex'
// import { SessionUser } from './session'


export class ImageService {
    constructor(private knex: Knex) {}

async handle(image: String){

    // AI handle 
    // let result = await ai.result;

    // return result
  }
}